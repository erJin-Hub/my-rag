# my-rag
一个用于学习的 RAG 项目

## 目录说明

- `step11_web_ui.py`：当前主入口，启动 FastAPI + Web UI。
- `steps/`：step1 到 step10 的学习脚本。
- `core/`：RAG 底层能力，如 embedding、索引、切分、rerank、memory。
- `services/`：接口业务逻辑。
- `schemas/`：接口请求/响应模型。
- `prompts/`：提示词模板。
- `web/`：原生 HTML/CSS/JS 前端页面。
